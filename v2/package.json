{
  "name": "agent-pool-v2",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "test": "bun test",
    "build": "bun build ./src/index.ts --outdir ./dist --target node"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
