{
  "name": "@agent-pool/orchestrator",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "dev": "bun run src/index.ts",
    "start": "bun run src/index.ts",
    "typecheck": "bunx --package typescript tsc -p tsconfig.json --noEmit --pretty false"
  },
  "dependencies": {
    "@agent-pool/auth": "workspace:*",
    "@agent-pool/config": "workspace:*",
    "@agent-pool/queue": "workspace:*",
    "@agent-pool/runtime": "workspace:*",
    "@agent-pool/shared": "workspace:*",
    "@agent-pool/storage": "workspace:*"
  }
}
