{
  "name": "@agent-pool/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "dev": "bun run src/index.ts",
    "start": "bun run src/index.ts",
    "typecheck": "bunx --package typescript tsc -p tsconfig.json --noEmit --pretty false"
  },
  "dependencies": {
    "@agent-pool/config": "workspace:*",
    "@agent-pool/db": "workspace:*",
    "@agent-pool/shared": "workspace:*",
    "express": "^5.1.0"
  }
}
